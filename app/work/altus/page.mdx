export const metadata = { title: "altus" };

# altus

Scaffold placeholder. This file proves the MDX pipeline compiles and that a
case study can be promoted by dropping an `.mdx` file into `app/work/`.

| Column | Value |
| --- | --- |
| GFM tables | working |
